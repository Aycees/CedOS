The default surface for any grouped content — dashboard tiles, list panels, settings sections. Flat: solid 1px border, 12px radius, card-color background, never a shadow.

```jsx
<Card><h2>Tasks</h2>...</Card>
```
