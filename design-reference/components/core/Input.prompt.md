```jsx
<Input variant="boxed" placeholder="Search notes" />
<Input variant="ghost" placeholder="Untitled" />
```
boxed: mono, 1px border, 8px radius — forms and search fields.
ghost: borderless serif, 24px — titles typed inline on the page (notes, events).
