```jsx
<Modal width={430} onClose={close}>
  <div className="kicker">NEW EVENT</div>
  <Input variant="ghost" placeholder="Event title" />
</Modal>
```
The only place a shadow appears in this system — everywhere else is flat.
